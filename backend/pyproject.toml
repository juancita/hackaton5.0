[project]
name = "muevete-cb-backend"
version = "0.1.0"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["pg: pruebas de integración contra PostgreSQL (docker compose up -d db)"]
